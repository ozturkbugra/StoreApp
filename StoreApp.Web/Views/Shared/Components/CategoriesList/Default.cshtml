
@model IEnumerable<CategoryViewModel>

<div class="d-grid gap-2">
    <a class="btn @(ViewBag.SelectedCategory == null ? "btn-dark":"btn-outline-dark")" href="/products">
        Ana Sayfa
    </a>

    @foreach(var item in Model)
    {
        <a class="btn @(ViewBag.SelectedCategory == item.Url ? "btn-dark":"btn-outline-dark")" asp-action="Index" asp-controller="Home" asp-route-category="@item.Url">
            @item.Name
        </a>
    }
</div>